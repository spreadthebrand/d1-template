export async function fetchYouTubeMetadata(_videoOrChannelUrl: string, _apiKey?: string) {
	return { available: false, message: "YouTube Data API placeholder for titles, thumbnails, and channel metadata; rankings should not use external stream counts without permission." };
}
