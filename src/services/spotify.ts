export async function fetchSpotifyMetadata(_artistOrTrackUrl: string, _accessToken?: string) {
	return { available: false, message: "Spotify metadata integration placeholder. Add official API credentials before use; do not infer stream counts." };
}
