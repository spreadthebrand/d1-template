export async function fetchAppleMusicMetadata(_artistOrSongUrl: string, _developerToken?: string) {
	return { available: false, message: "Apple Music metadata integration placeholder. Add official API credentials before use; public arbitrary stream counts are not promised." };
}
