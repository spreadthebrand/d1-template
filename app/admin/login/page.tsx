"use client";
import { signIn } from "next-auth/react";
import { useState } from "react";
export default function Login(){ const [error,setError]=useState(''); async function onSubmit(event:React.FormEvent<HTMLFormElement>){ event.preventDefault(); const formData=new FormData(event.currentTarget); const res=await signIn('credentials',{ email:formData.get('email'), password:formData.get('password'), redirect:false, callbackUrl:'/admin' }); if(res?.ok) location.href='/admin'; else setError('Invalid credentials'); } return <section className="section grid place-items-center"><form onSubmit={onSubmit} className="card w-full max-w-md"><h1 className="text-3xl font-black">Admin / Creator Login</h1><label className="mt-6 block"><span className="label">Email</span><input name="email" type="email" className="input mt-2"/></label><label className="mt-4 block"><span className="label">Password</span><input name="password" type="password" className="input mt-2"/></label><button className="btn-primary mt-6 w-full">Sign in</button>{error && <p className="mt-4 text-red-300">{error}</p>}</form></section> }
