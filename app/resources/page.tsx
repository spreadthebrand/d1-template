export const dynamic = "force-dynamic";
import { prisma } from "@/lib/prisma"; import { ResourceCard } from "@/components/Cards";
export default async function Resources(){ const articles: any[]=await prisma.resourceArticle.findMany({ where:{ isPublished:true }, orderBy:{ createdAt:"desc" } }); return <section className="section"><h1 className="text-5xl font-black">Creator Resources</h1><p className="mt-5 max-w-3xl text-slate-300">Original guides for launching web-first digital networks responsibly.</p><div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{articles.map((a: any)=><ResourceCard key={a.id} article={a}/>)}</div></section> }
