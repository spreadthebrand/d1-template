import { NextResponse } from "next/server"; import { prisma } from "@/lib/prisma"; import { requireRole } from "@/lib/apiAuth";
export async function GET(_:Request,{params}:{params:{networkId:string}}){ const auth=await requireRole("creator"); if(auth.error) return auth.error; const network=await prisma.network.findFirst({where:{id:params.networkId,ownerId:auth.session!.user!.id!}}); if(!network) return NextResponse.json({error:"Not found"},{status:404}); const checklist=await prisma.distributionChecklist.upsert({where:{networkId:network.id},create:{networkId:network.id},update:{}}); return NextResponse.json(checklist); }
