import Link from "next/link";
import { StatusBadge } from "./StatusBadge";
type Network = { slug: string; name: string; logoUrl?: string | null; category: string; description: string; status: string; owner?: { name?: string | null; creatorProfile?: { organization?: string | null } | null } | null };
export function NetworkCard({ network }: { network: Network }) { return <article className="card group"><div className="flex items-start gap-4"><div className="grid h-16 w-16 shrink-0 place-items-center rounded-2xl bg-gradient-to-br from-electric to-gold text-xl font-black text-navy-950">{network.logoUrl ? "◎" : network.name.slice(0,2)}</div><div><div className="flex flex-wrap items-center gap-2"><h3 className="text-xl font-black group-hover:text-gold">{network.name}</h3><StatusBadge status={network.status}/></div><p className="mt-1 text-sm text-electric">{network.category}</p><p className="mt-3 line-clamp-3 text-slate-300">{network.description}</p><p className="mt-3 text-sm text-slate-500">Founder: {network.owner?.creatorProfile?.organization ?? network.owner?.name ?? "CCB Creator"}</p></div></div><div className="mt-6 flex gap-3"><Link href={`/networks/${network.slug}`} className="btn-primary py-2">Watch</Link><Link href="/contact" className="btn-secondary py-2">Apply/contact</Link></div></article>; }
