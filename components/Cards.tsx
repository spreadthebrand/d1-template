import Link from "next/link";
import { StatusBadge } from "./StatusBadge";
export function NetworkCard({ network }: { network: any }) { return <article className="card"><div className="flex items-start gap-4"><div className="grid h-16 w-16 shrink-0 place-items-center rounded-2xl bg-gradient-to-br from-electric to-gold font-black text-navy-950">{network.name.slice(0, 2)}</div><div><div className="flex flex-wrap items-center gap-2"><h3 className="text-xl font-black">{network.name}</h3><StatusBadge status={network.status}/></div><p className="mt-1 text-sm text-electric">{network.category}</p><p className="mt-3 line-clamp-3 text-slate-300">{network.description}</p><p className="mt-3 text-sm text-slate-500">Founder: {network.owner?.creatorProfile?.organization ?? network.owner?.name ?? "CCB Creator"}</p></div></div><div className="mt-6 flex gap-3"><Link href={`/networks/${network.slug}`} className="btn-primary py-2">Watch</Link><Link href="/contact" className="btn-secondary py-2">Apply/contact</Link></div></article>; }
export function VideoCard({ video }: { video: any }) { return <Link href={`/watch/${video.slug}`} className="card block hover:border-electric/50"><div className="grid aspect-video place-items-center rounded-2xl bg-gradient-to-br from-navy-800 to-black text-5xl">▶</div><p className="mt-4 text-xs font-bold uppercase tracking-widest text-gold">{video.category}</p><h3 className="mt-2 text-xl font-black">{video.title}</h3><p className="mt-1 text-sm text-slate-400">{video.network?.name}</p><p className="mt-3 line-clamp-2 text-sm text-slate-300">{video.description}</p></Link>; }
export function ResourceCard({ article }: { article: any }) { return <Link href={`/resources/${article.slug}`} className="card block hover:border-electric/50"><p className="text-xs font-bold uppercase tracking-widest text-gold">{article.category}</p><h3 className="mt-2 text-2xl font-black">{article.title}</h3><p className="mt-3 text-slate-300">{article.excerpt}</p></Link>; }
export function AnalyticsCards({ stats }: { stats: Record<string, number | string> }) { return <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(stats).map(([key, value]) => <div key={key} className="card"><p className="text-sm capitalize text-slate-400">{key.replaceAll("_", " ")}</p><p className="mt-2 text-4xl font-black text-gold">{value}</p></div>)}</div>; }
