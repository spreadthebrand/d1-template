import Link from "next/link";
type Plan = { name: string; priceLabel: string; description: string; features: string[]; ctaLabel: string };
export function PricingCards({ plans }: { plans: Plan[] }) { return <div className="grid gap-6 md:grid-cols-2 xl:grid-cols-4">{plans.map(plan => <article key={plan.name} className="card flex flex-col"><h3 className="text-2xl font-black">{plan.name}</h3><p className="mt-2 text-3xl font-black text-gold">{plan.priceLabel}</p><p className="mt-3 text-slate-300">{plan.description}</p><ul className="mt-6 flex-1 space-y-3 text-sm text-slate-200">{plan.features.map(f => <li key={f} className="flex gap-2"><span className="text-electric">✓</span>{f}</li>)}</ul><Link href="/apply" className="btn-primary mt-6">{plan.ctaLabel}</Link></article>)}</div>; }
