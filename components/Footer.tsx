import Link from "next/link";
export function Footer() { const groups = [["Platform", ["Start", "Distribution", "Pricing", "Watch", "Deploy"]], ["Company", ["About", "Contact", "Resources"]], ["Legal", ["Privacy", "Terms", "Content Rights"]]] as const; return <footer className="border-t border-white/10 bg-black"><div className="section grid gap-10 md:grid-cols-4"><div><h3 className="text-2xl font-black">CCB Network</h3><p className="mt-3 text-slate-400">A web-first launch platform for creators preparing long-term media distribution.</p></div>{groups.map(([title, items]) => <div key={title}><h4 className="font-bold text-gold">{title}</h4><div className="mt-4 grid gap-2 text-slate-400">{items.map((item) => <Link key={item} href={`/${item.toLowerCase().replaceAll(" ", "-")}`} className="hover:text-white">{item}</Link>)}</div></div>)}</div><p className="px-4 pb-8 text-center text-sm text-slate-500">© {new Date().getFullYear()} CCB Network. TV app distribution is a roadmap capability requiring separate approvals, accounts, fees, and compliance.</p></footer>; }
