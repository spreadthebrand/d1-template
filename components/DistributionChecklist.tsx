const items = ["Branding","Content library","Trailer","Rights confirmation","Monetization plan","Audience strategy","Privacy policy","Terms"];
export function DistributionChecklist({ completed = 3 }: { completed?: number }) { return <div className="card"><h3 className="text-2xl font-black">Distribution Readiness Checklist</h3><p className="mt-2 text-slate-400">Connected-TV expansion is reviewed after web launch foundations are ready.</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{items.map((i,idx) => <div key={i} className="flex items-center gap-3 rounded-2xl bg-white/5 p-3"><span className={idx < completed ? "text-electric" : "text-slate-600"}>{idx < completed ? "✓" : "○"}</span><span>{i}</span></div>)}</div></div>; }
