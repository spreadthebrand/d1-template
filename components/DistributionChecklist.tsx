const items = ["Branding", "Content library", "Trailer", "Rights confirmation", "Monetization plan", "Audience strategy", "Privacy policy", "Terms"];
export function DistributionChecklist({ completed = 3 }: { completed?: number }) { return <div className="card"><h3 className="text-2xl font-black">Distribution Readiness Checklist</h3><p className="mt-2 text-slate-400">Connected-TV expansion is reviewed after web launch foundations are ready.</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{items.map((item, index) => <div key={item} className="flex gap-3 rounded-2xl bg-white/5 p-3"><span className={index < completed ? "text-electric" : "text-slate-600"}>{index < completed ? "✓" : "○"}</span>{item}</div>)}</div></div>; }
