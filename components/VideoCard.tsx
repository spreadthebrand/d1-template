import Link from "next/link";
type Video = { slug: string; title: string; thumbnailUrl?: string | null; category: string; description: string; network?: { name: string } | null };
export function VideoCard({ video }: { video: Video }) { return <Link href={`/watch/${video.slug}`} className="card group block"><div className="aspect-video overflow-hidden rounded-2xl bg-gradient-to-br from-navy-800 to-black"><div className="grid h-full place-items-center bg-electric/10 text-5xl transition group-hover:scale-105">▶</div></div><p className="mt-4 text-xs font-bold uppercase tracking-widest text-gold">{video.category}</p><h3 className="mt-2 text-xl font-black group-hover:text-electric">{video.title}</h3><p className="mt-1 text-sm text-slate-400">{video.network?.name}</p><p className="mt-3 line-clamp-2 text-sm text-slate-300">{video.description}</p></Link>; }
